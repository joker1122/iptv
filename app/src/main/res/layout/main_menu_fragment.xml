<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/head_bar_height"
        android:background="@color/head_bar_background"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/edit_search"
            android:layout_width="@dimen/search_edit_width"
            android:layout_height="@dimen/search_edit_height"
            android:layout_marginLeft="@dimen/search_to_left"
            android:background="@drawable/shape_edit" />

        <ImageView
            android:id="@+id/bt_search"
            android:layout_width="@dimen/search_icon_width"
            android:layout_height="@dimen/search_icon_height"
            android:background="@drawable/shape_icon"
            android:padding="6dp"
            android:src="@drawable/ic_search" />

        <TextView
            android:layout_width="@dimen/search_to_fav"
            android:layout_height="match_parent" />

        <ImageView
            android:id="@+id/icon_fav"
            android:layout_width="@dimen/fav_icon_width"
            android:layout_height="@dimen/fav_icon_height"
            android:layout_marginRight="@dimen/fav_to_right"
            android:src="@drawable/ic_fav" />
    </LinearLayout>

    <ListView
        android:id="@+id/lv_message_about_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>
